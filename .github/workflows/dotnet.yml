# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        dotnet-version: [ '9.0.x' ]

    steps:
    - uses: actions/checkout@v4

    - name: Print current directory
      run: pwd

    - name: List files in the current directory
      run: ls -la

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ matrix.dotnet-version }}

    - name: Display dotnet version
      run: dotnet --version

    - name: Restore dependencies
      run: dotnet restore Devops.sln
    - name: Build
      run: dotnet build Devops.sln --no-restore
    - name: Test
      run: dotnet test Devops.sln --no-build --verbosity normal --logger trx --results-directory "TestResults-${{ matrix.dotnet-version }}"
    
    # - name: SCP Command to Transfer Files
    #   uses: appleboy/scp-action@v0.1.7
    #   with:
    #     host: ${{ secrets.VPS_HOST }}
    #     username: ${{ secrets.VPS_USERNAME }}
    #     password: ${{ secrets.VPS_VPS_PASSWORD }}
    #     key: ${{ secrets.SSH_KEY }} 
    #     source: "tests/a.txt,tests/b.txt"
    #     target: /var/devops
    
    - name: Restart service on VPS
      uses: appleboy/ssh-action@v0.1.10
      with:
        host: ${{ secrets.VPS_HOST }}
        username: ${{ secrets.VPS_USER }}
        password: ${{ secrets.VPS_VPS_PASSWORD }}
        key: ${{ secrets.SSH_KEY }}
        script: |
          echo "发布完成，开始重启服务 🚀"
          sudo systemctl restart myapp.service
          echo "服务已重启 🎉"
  
        